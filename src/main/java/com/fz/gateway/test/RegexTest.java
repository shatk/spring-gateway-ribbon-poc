package com.fz.gateway.test;

import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class RegexTest {

	/**
	 * public static void main( String args[] ) { // String to be scanned to find the
	 * pattern. String line = "http://localhost:8080/mobile/v1/booking/api2/flights/1";
	 * //String pattern = "(.*)(\\d+)(.*)"; String pattern1 =
	 * "mobile/v1/booking/(?<segment>.*)"; String pattern2 = "api2/flights/1";
	 *
	 * // Create a Pattern object Pattern p1 = Pattern.compile(pattern1); //Pattern p2 =
	 * Pattern.compile(pattern2);
	 *
	 * // Now create matcher object. Matcher matcher1 = p1.matcher(line); //Matcher
	 * matcher2 = p2.matcher(line);
	 *
	 * if (matcher1.find( )) { System.out.println("Current value: " + line );
	 * System.out.println("Pattern : " + matcher1.pattern() ); System.out.println("New
	 * value: " + matcher1.replaceAll(pattern2) ); //System.out.println("Found value: " +
	 * m.group(1) ); //System.out.println("Found value: " + m.group(2) ); }else {
	 * System.out.println("matcher1 : NO MATCH"); } }
	 **/

}
